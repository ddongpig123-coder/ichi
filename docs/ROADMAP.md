# ichi 개발 로드맵 (2026.07 ~ 2027.04)

> 기준일: 2026-07-08 / 목표: **2027-01-15 App Store 제출** → 2월 승인 → 4월 신입생 시즌 승부
>
> 전략 요약: "일본 유학생판 에브리타임". 한국인 유학생(메이지대)에서 시작 →
> 유학생 언어권 확장(중국어권 등)이 본 게임. 일본인은 언어교환 등 "다리 기능"으로만.
> 익명성 유지 + 낮은 진입장벽(게스트 입장) + 나중에 조일 수 있는 구조(verificationLevel).

---

## 0. 역산 스케줄 (왜 이 일정인가)

| 시점 | 이벤트 | 근거 |
|---|---|---|
| 2027-04 | 신입생 시즌 (최대 승부처) | 4월 입학. 합격자는 2~3월부터 정보 탐색 시작 |
| 2027-02 중순 | 스토어 공개 상태 도달 | 합격자 유입 채널 공략 시작점 |
| **2027-01-15** | **App Store 제출** | 익명 UGC 앱은 리젝 1~2회 각오 → 심사 버퍼 4주 |
| 2026-12-31 | **기능 동결** (이후 버그픽스만) | 제출 2주 전 QA 전용 기간 확보 |
| 2026-12 초 | TestFlight 베타 시작 | 협력자 10~20명 실사용 피드백 |
| 2026-08~11 | 기능 개발 본기간 (4개월) | 아래 월별 계획 |
| 2026-07 | Phase 0: 설계 확정 | 코드 최소, 결정 최대 |

주의: 1월 말~2월 초는 일본 대학 후기 시험기간. **제출(1/15)을 시험 전에 끝내는 구조**로 짰다.
12월 말 연말연시(~1/3)는 작업 제외 주간으로 계산에서 뺐다.

---

## 1. 월별 계획

### 7월 (Phase 0 — 설계 확정. 남은 3주, 가벼움)

목표: **코드를 적게 쓰고 결정을 많이 한다.** 시험기간(7월 말)과 겹치므로 세션 부담 최소.

- [x] Firestore 보안 규칙 프로덕션 배포 (완료 7/6)
- [x] 규칙 확장판 작성·커밋 — Phase 1~2 컬렉션 선행 정의 (완료 7/8)
- [x] `UserProfile`에 `verificationLevel`, `language`, `department` 필드 추가 + 규칙 배포 (완료 7/11)
- [x] `courses` / `reviews`(강의평) TypeScript 타입 정의 (완료 7/11, `src/types/course.ts`)
- [x] 신고/차단 데이터 흐름 설계 문서 (완료 7/11, `docs/MODERATION.md`)
- [x] 이용약관·개인정보처리방침 초안 (완료 7/11, `docs/legal/`) — 자리표시자([■]) 3종:
      운영자 명의 / 문의 이메일 / 관할 법원. 12월 스토어 준비 때 확정·웹 게시
- [x] CLAUDE.md 확장: 프로젝트 규칙·아키텍처 결정사항 기록 (완료 7/11, `AGENTS.md`)

**예상 세션: 주 2회 × 3주 = 6세션.** 문서·타입 정의 위주라 세션당 토큰 소모 적음.

### 8월 (Phase 1a — 목업 → 실데이터 전환. 여름방학, 최대 작업량 가능)

목표: **앱의 뼈대를 진짜 데이터로 교체.** 이번 프로젝트에서 가장 큰 공사.
UI는 이미 완성되어 있으므로 데이터 레이어만 갈아끼우는 작업.

- 1주차: 회원가입/로그인 화면 정비 (이메일 + Microsoft 인증), `users` 문서 생성 흐름
  - [x] 익명 포함 첫 진입 시 `users` 문서 자동 생성 (완료 7/12, `ensureUserProfile`)
  - [x] Firebase 콘솔 Email/Password 인증 활성화 (완료 7/12)
  - [x] 계정 연결: 게스트→이메일 승격 시 uid 유지 (완료 7/12, `linkAnonymousWithEmail`
        + `app/account.tsx` 등록/로그인/로그아웃 화면 + 프로필 탭 계정 상태 행.
        실서버 검증: 동일 uid로 익명→password 전환 확인)
  - [x] Microsoft 인증을 계정 연결(linkWithPopup) 방식으로 재구현 (완료 7/14 — 기존
        코드는 토큰 교환 없는 미완성 스캐폴드였음. Azure 리다이렉트 설정은 실테스트로
        검증 완료. ~.ac.jp면 schoolDomain 자동 기록. **웹 전용**, 네이티브는 9월 2주차)
  - [x] 폰(Expo Go) 게시판 탭 크래시 수정 (완료 7/14 — async-storage 버전 불일치.
        `npx expo install --fix`로 SDK 56 정렬 + usePinnedBoards 에러 내성)
  - [x] 네이티브(Expo Go) 동작 확인 (완료 7/14 — 터널 모드 폰 테스트, 게시판 탭
        크래시 수정 후 정상 동작 확인)
  - 참고: Microsoft 로그인은 M365 도입 대학의 계정만 가능 (개인/비M365 대학 메일은
    Microsoft 측에서 거부 — 정상 동작. 충북대 메일로 확인됨)
- 2주차: 시간표 실데이터화 — `users/{uid}/timetables/{semester}` 구조로 저장/로드,
  기존 학기 선택기·셀 편집 UI에 연결
  - [x] `timetableService` + 홈 화면 Firestore 전환 (완료 7/15 — 목업 제거,
        visibility: private 초기화, 실서버 저장/로드 사이클 검증. 웹 확인 완료,
        폰 확인은 3주차 작업과 묶어서)
- 3주차: 친구 실데이터화 — `friendships`/`friendRequests` 컬렉션,
  기존 AddFriendModal의 stub(`friendRequestService.ts`)을 실제 Firestore 쿼리로 교체,
  친구 수락 UI(받은 신청 목록) 신규 제작
  - [x] 완료 7/15 — usersPublic/emailIndex 신설(users 본체 비공개 유지하며 검색 가능),
        신청·수신함·승인·거부·삭제 전부 실구현, FriendsContext 실데이터화(표시순 영속화)
- 4주차: 친구 시간표 겹침 표시를 실데이터 기반으로 재계산, 통합 테스트, 버그픽스
  - [x] 완료 7/15 — useFriendOverlaps(曜日+時限+講義名 대조), 목업 전면 제거,
        시간표 기본 공개범위 friends로 결정(§5 참조), 실서버 2계정 E2E 통과
  - [ ] 폰(Expo Go) 통합 테스트 — 친구 추가~겹침 표시 흐름 재확인

**예상 세션: 주 4회 × 4주 = 16세션.** 각 주가 독립된 기능 단위라 세션 경계가 깔끔함.

### 9월 (Phase 1b — 진입장벽 낮추기 + 안전장치)

목표: **게스트 입장 + 신고/차단.** 스토어 심사 요건의 핵심을 이 달에 끝냄.

- 1주차: 온보딩 재설계 — 동의(약관) → 언어 선택(한/일) → 학교 선택 → 바로 사용.
  i18n 라이브러리 도입 + 기존 하드코딩 일본어 문자열 추출 시작
  - [x] 완료 7/15 — 온보딩 3단계(EULA 동의/언어/학교), 약관·방침 전문 화면,
        학교 마스터(schools.ts), 자체 i18n(외부 의존성 0, 타입 안전 ja/ko 사전),
        탭 라벨 적용. 웹 E2E 검증(첫 진입 강제→한국어 전환→재진입 스킵)
- 2주차: i18n 마무리 (전 화면 한/일 전환), Microsoft 학교계정 즉시 인증(선택) + 인증 배지 표시
  - [x] i18n 문자열 추출 완료 (7/16) — 화면·컴포넌트 32개 파일 전부 t() 전환(~190키),
        timeAgo/요일 표기 헬퍼 일원화, 프로필에 언어 토글(🌐) 추가, Microsoft 버튼
        "認証バッジ付与" 라벨 적용(사전 키). 웹 E2E: 온보딩 한국어→전 탭 확인, 토글 왕복.
        잔여(데이터 마스터): 게시판/라운지/테마 이름의 ko 필드는 별도 작업
  - [x] 학교 이메일 인증 링크 완료 (7/16) — sendEmailVerification 자동 발송(대학 메일 등록 시),
        계정 화면에 인증 섹션(발송/확인 버튼), 인증 배지(계정+프로필). 배지 판정은
        `대학 메일 && (emailVerified || Microsoft 연계)` — verificationLevel 승격은 서버 책무 유지.
        실메일 수신 왕복 테스트는 실계정으로 확인 필요
  - [x] Microsoft 버튼 "認証バッジ付与" 라벨 적용 (7/16, i18n 사전 키)
  - [x] Microsoft 네이티브(폰) 연동 — **12월로 이관 확정 (7/16 태희 결정)**.
        Firebase JS SDK는 Microsoft 토큰의 signInWithCredential 검증 미지원(7/14 확인)이라
        클라이언트 단독 구현 불가. dev-client(EAS build) + @react-native-firebase 전환으로
        12월 1주차 EAS Build 작업에 흡수. 폰에서는 학교 이메일 인증이 배지 경로를 커버하므로
        그때까지 공백 없음 (Cloud Functions 커스텀 토큰 중계안은 Blaze 요금제 필요라 기각)
  - **→ 9월 2주차 실행 항목 전부 완료 (7/16).** 잔여 후속: 실계정 메일 인증 왕복 테스트,
    게시판/라운지/테마 마스터 데이터의 ko 이름 필드
- 3주차: 신고 기능 (글/댓글/쪽지 신고 → `reports` 컬렉션), 차단 기능 (차단 유저 콘텐츠 필터링)
  - [x] 완료 (7/14) — `reportService`/`blockService`, `BlockContext`(앱 전역 차단목록),
        공통 `ModerationMenu`(通報 이유 6종 + ブロック 확인), 게시글/댓글/쪽지 렌더 시
        차단 유저 접기, 프로필 → ブロックリスト 관리 화면. 웹 미리보기로 通報 송신·라운지
        작성 왕복까지 검증. (금칙어 경고·EULA 온보딩·soft delete는 MODERATION 체크리스트에 잔여)
- 4주차: 유학생 라운지 게시판 (전국 단위 `lounges`: 비자/알바/부동산/중고/취업) —
  기존 게시판 컴포넌트 재사용이라 작업량 중간
  - [x] 완료 (7/14) — `LOUNGES` 5종(ビザ/アルバイト/住まい/売買/就活), `loungeService`,
        `app/lounge/` 스택(카테고리 목록·글목록·작성·상세), 게시판 탭 상단 진입 배너.
        차단 필터·通報 메뉴 연동. 실서버 작성→목록 반영 확인

**예상 세션: 주 3~4회 × 4주 = 14세션.** i18n 문자열 추출은 기계적 작업이라 세션당 처리량 큼.

### 10월 (Phase 1c — 강의 데이터 기반 구축)

목표: **시라버스 크롤러 + 강의 검색 추가 UX.** 선배 시간표(11월)의 전제조건.

- 1주차: ✅ **완료 (7/18, 준희)** 메이지대 크롤러 제작 (`scripts/crawl-syllabus.mjs`) —
  리스트+상세크롤(単位数·sourceUrl), 저속 크롤링+재시도, Course 타입 준수.
  "1000건 제한"은 실측상 없음(전 페이지 접근 가능). 적재 스크립트(`load-firestore.mjs`,
  dry-run 지원)까지 작성됨. 상학부 2026 春(1,349과목)+秋(1,354과목) 수집·검증 완료
  (scripts/output/, git 제외).
- 2주차: 상학부 → 전 학부 확대 적재, 데이터 정규화(전각/반각, 교수명 표기 통일), 검증
  - **schoolDomain = `meiji.ac.jp` 확정** (7/18 태희·준희 합의). `load-firestore.mjs` 기본값 반영됨
  - 전각공백 정규화는 크롤러에 내장됨
  - [x] 전 학부 규모 조사: 10학부 × 2학기 = **21,944과목** (法1,077/1,073 商1,376/1,380
        政経1,229/1,251 文1,702/1,602 理工1,603/1,583 農861/827 経営928/951
        情コミ657/638 国際日本872/904 総合数理722/708)
  - [x] ✅ **전 학부 수집 완료 (7/20)** — 20/20 파일, **20,022과목, credits 99.9%**(20,007건),
        id 중복 0, 전 파일 `load-firestore --dry-run` 검증 통과.
        학부별(春/秋): 法1070/1066 商1349/1354 政経1285/1312 文1576/1481 理工1303/1273
        農583/561 経営875/884 情コミ632/615 国際日本819/849 総合数理568/567
  - [x] 通年 과목은 春·秋 양쪽 등록(B안), id는 `{courseNumber}-{semester}-{day}{period}`
  - 의도적 제외: 요일·교시 없는 과목(집중강의·실습·대학원〔Ｍ〕), 일요일 과목 — scripts/README 참조
  - [ ] **Firestore 적재는 검색 UX 착수 직전에 실행** (서비스계정 키만 있으면 즉시 가능)
  - [x] `load-firestore.mjs`에 `nameGrams`(2-gram) 추가 + `confirmCount` 제거 (7/21).
        합성 픽스처로 dry-run 검증 완료 (실크롤 JSON은 데스크톱 PC에만 있음)
  - [x] ✅ **Firestore 적재 완료 (7/22, 준희)** — 상학부(12) 秋 1,354건 선행 적재 →
        Firestore에서 `nameGrams` array-contains 검색 동작 확인(マーケ50/会計20/企業5) →
        전 학부 20,022건 일괄 적재. 경로 `schools/meiji.ac.jp/departments/{dept}/courses`
        - 버그 수정: firebase-admin **v14 ESM은 `admin.credential.cert`가 undefined** →
          modular API(`firebase-admin/app`)로 교체 (커밋 14953e7). 태희도 적재 시 동일 코드 사용
        - 서비스 계정 키는 각자 발급·로컬 보관 (전송 금지, 7/18 원칙) — `scripts/keys/`는 gitignore
        - 재적재는 문서ID 동일하므로 멱등(덮어쓰기) — 안전하게 반복 가능
- 3주차: 앱에 "강의 검색 → 탭 추가" UX — 학부 선택, 검색, 시간표에 원탭 등록
  (수동 입력은 폴백으로 유지, 입력 데이터는 `verified:false`로 courses에 승격 대기)
  - [x] ✅ **화면 구현 완료 (7/21)** — `app/course-search.tsx`(학부 칩 10개·검색·결과·원탭 등록),
        `courseService`(Firestore 전방일치 + 목업 폴백), `departments.ts`, i18n ja/ko 18키.
        홈(시간표)에 진입 버튼 + 복귀 시 해당 학기 재로드. 웹 E2E: 강의명/교수명 검색,
        시간표 반영(月2), 중복 시간대 차단까지 확인
  - [ ] **적재 후 `courseService.COURSE_DATA_LOADED = true`로 전환** (현재 목업 표시 중.
        전환하면 `src/data/mockCourses.ts` 삭제 가능)
  - [x] ~~전방일치 한계~~ → **2-gram 방식으로 해결 확정 (7/21 통화)**. 적재 측은 반영 완료.
        앱 측 `courseService`는 아직 전방일치 구현 상태 — 아래 인계 항목 참조

#### 7/21 통화 합의 · 담당 재조정

**강의검색·시간표에 붙는 것은 전부 태희 축으로 통합.** 강의검색과 학점관리가 둘 다
시간표 + courses에 붙으므로 한 사람이 쥐는 게 맞다는 판단. 준희는 Phase 1b 잔여로 이동.

태희 인계 대상 (준희가 7/21까지 만든 것, junhee 브랜치에 푸시됨):
- `app/course-search.tsx` — 화면 완성, 웹 E2E 검증 완료
- `src/services/courseService.ts` — **전방일치 구현 상태.
  `nameGrams` array-contains + 정규화 재필터로 교체 필요**
- `src/data/departments.ts`, `src/data/mockCourses.ts`
- `app/(tabs)/index.tsx` 3곳 (검색 진입 버튼 / 복귀 시 재로드 / 높이 상수)
- `SessionFormModal`의 수동 입력 → `verified:false` 승격 등록 (미착수)

⚠️ **인계 시 주의**: `mockCourses.ts`의 12건은 `confirmCount`를 갖고 있다.
태희가 `Course`에서 `confirmCount`를 제거하면 **이 파일에서 타입 에러**가 난다.
적재 완료 후 `courseService.COURSE_DATA_LOADED = true`로 바꾸고 파일째 삭제하면 같이 해결됨.

준희 담당: Phase 1b 잔여 (soft delete, 금칙어 경고)
  - [x] ✅ **완료 (7/21)** — soft delete(게시판·라운지, 규칙 변경 불필요) + 금칙어 경고
        (ja/ko 시드 리스트, 정규화로 「병 신」류 회피 방어). 웹 E2E 검증 완료.
        상세는 MODERATION.md §6. EULA 항목은 이미 완료돼 있어 체크리스트만 정정
  - [ ] 잔여: 규칙에서 `allow delete` 제거(태희 창구), 금칙어 리스트 확충, 쪽지 미적용
- 4주차: 크라우드소싱 승격 로직 (동일 강의 N명 확인 시 verified), 버퍼 주간
  - [x] **설계 확정 (7/21 통화)** — courses는 `update: false` 유지(자작자연 원천 차단,
        Blaze 불필요). 대신 `courses/{id}/confirms/{uid}` 서브컬렉션을 create/delete-only로
        두고 **문서ID = uid로 1인 1회를 구조 강제**. `Course.confirmCount` 필드는 제거하고,
        승격 판정은 클라이언트가 confirms 개수를 세서 파생 (courses.verified 승격 자체를 안 함)

**예상 세션: 주 3회 × 4주 = 12세션.** 크롤러는 로컬 스크립트라 미리보기 검증 불필요 → 토큰 저렴.

### 11월 (Phase 2 — 킬러 기능)

목표: **선배 시간표 공개 + 강의 한줄평.** 차별화 기능 완성.

- 1주차: 시간표 공개 범위 설정 UI (비공개/친구/학부/전체) — 규칙은 이미 배포되어 있음
- 2주차: 선배 시간표 열람 화면 — 같은 학부 공개 시간표 리스트, 학년 필터
- 3주차: 강의 한줄평 v1 — 구조화 평가(별점 + 태그: 単位 난이도/출석 확인/유학생 친화) 필수,
  자유텍스트(언어 필드 포함) 선택
- 4주차: 강의 상세 화면(시라버스 정보 + 평점 집계 + 리뷰 목록), 신고 연동

**예상 세션: 주 3~4회 × 4주 = 14세션.**

### 12월 (QA + 스토어 준비. 12/29~1/3 연말연시 제외)

목표: **기능 추가 없이 완성도만.** 12/31 기능 동결.

- 1주차: TestFlight 베타 배포 (EAS Build), 협력자 10~20명 모집·초대 (유학생회 접촉은 11월부터)
  - EAS Build 전환 시 Microsoft 네이티브 연동 포함 — dev-client + @react-native-firebase/auth로
    구현 (9월 2주차에서 이관. Expo Go + Firebase JS SDK로는 불가 — 7/14 확인)
- 2주차: 베타 피드백 반영 라운드 1 — 크래시/치명 버그 우선
- 3주차: 스토어 자산 — 스크린샷(일/한), 앱 설명, 개인정보처리방침 웹페이지 게시,
  심사용 데모 계정 준비, 연령 등급 설문
- 4주차 (연말연시 전): 심사 요건 최종 점검 — 신고 후 24시간 대응 체계 문서화,
  EULA 동의 화면, 콘텐츠 필터링 동작 확인. **기능 동결.**
  - Microsoft 단독 등록자에게 "졸업 전 개인 이메일 추가 연결" 안내 문구/동선 확인
    (대학 계정은 졸업 시 회수됨 — 개인 이메일이 없으면 로그인 수단 상실)

**예상 세션: 주 3회 × 3.5주 = 10세션.** 스토어 자산 제작은 Claude가 문안·정책 페이지 생성 가능.

### 1월 (제출 + 심사 대응)

- 1/5~1/12: 베타 피드백 반영 라운드 2 (버그픽스만), 최종 빌드
- **1/15: App Store 제출** (+ Google Play 동시 제출)
- 1/16~2월 초: 리젝 대응 대기. 리젝 사유는 대부분 익명 UGC 관련 → 신고/차단/약관은
  이미 구현되어 있으므로 메타데이터 수정 수준의 대응 예상. 시험기간과 겹치므로
  개발 작업은 리젝 대응만
- 병행: 시딩 콘텐츠 준비 — 협력자 선배 시간표·강의평 채워넣기 (콘시어지 작업, 개발 아님)

### 2~3월 (시딩·마케팅 — 개발 최소)

- 승인 → 스토어 공개. 동유모/일한모 홍보 글, 합격자 오픈채팅, 어학원 채널 공략
- 개발: 크래시 대응 + 소규모 개선만. **새 기능 금지** (안정성이 첫인상)
- 3월 말: 2027년도 시라버스 재크롤링 (신년도 강의 데이터 갱신)

### 4월 (신입생 시즌 — 판정)

- 목표: 메이지대 한국인 유학생 **주간 활성 100명**
- 5월에 리텐션(주간 재방문율)으로 성패 판정 → 확장(중국어권/타 대학) 또는 원인 분석

### 런칭 후 (Phase 3+, 5월 이후) — 학점 관리(졸업요건 추적)

**1.0 범위 밖.** 설계·상학부 마스터는 확정됨(§5 · [CREDIT-TRACKING.md](CREDIT-TRACKING.md)).
2-트랙으로 개발: **트랙 A** = 便覧 → LLM 초안 → 검수 → 공유 마스터(오프라인 authoring),
**트랙 B** = 학생 手입력(성적표 이수 이력). 런타임 계산은 결정론(LLM 0).
최소 v1: 프로필 학부·입학년도 선택 → 마스터 로드 → 성적 手입력 → 영역별 진행률 대시보드.

---

## 2. 주간 리듬 (하루 단위 템플릿)

두 사람 모두 학업 병행 전제. **주 3~4회, 1회 2~3시간** 작업 세션.

```
[세션 시작 10분]
  git pull → 상대방 브랜치 변경 확인 → 오늘의 기능 단위 1개 선언
[본작업 1.5~2시간]
  Claude와 구현 → 미리보기 검증 → npx tsc --noEmit
[세션 마감 20분]
  커밋(기능 단위) → 푸시 → 필요시 상대방에게 머지 요청 한 줄 공유
```

- **화·목·토** 같은 고정 요일 권장 (리듬이 곧 진도)
- 두 사람 분담: 한 명이 "데이터/백엔드 축"(8월 전환, 10월 크롤러),
  다른 한 명이 "UI/콘텐츠 축"(9월 i18n·온보딩, 12월 스토어 자산) 식으로
  **브랜치 충돌이 안 나는 축 분리** 유지 (지금 taehui/junhee 분담 방식 그대로)

---

## 3. Claude 세션 운영 원칙 (토큰 효율)

월 40~60세션이 아니라 **월 12~16세션으로 계획한 이유**가 이것.

1. **1세션 = 1기능 단위.** "시간표 저장 구현"처럼 커밋 하나로 끝나는 크기.
   세션이 길어져 컨텍스트가 커지면 요약 비용이 커지므로, 기능이 끝나면 세션도 끝냄
2. **컨텍스트는 문서로 외부화.** 아키텍처 결정·규칙은 CLAUDE.md와 docs/에 커밋
   → 매 세션 재설명 불필요 (이 문서 자체가 그 역할)
3. **검증은 저비용 수단부터.** `tsc --noEmit`(저렴) → 미리보기 스냅샷(중간) →
   스크린샷(비쌈) 순. 스크린샷은 UI 변경 시에만
4. **크롤러·스크립트류는 토큰 우대 작업.** 로컬 실행 결과만 확인하면 되므로
   미리보기 왕복이 없음 → 10월이 세션 수 대비 산출물이 가장 큰 달
5. **대형 작업(8월 전환)은 계획 세션 1회 분리.** 스키마·파일 목록을 먼저 확정하고
   구현 세션은 그 계획을 따라가기만 → 재작업 토큰 낭비 방지
6. **막히면 세션을 갈아탄다.** 같은 버그로 3회 이상 왕복하면 새 세션에서
   증상 요약부터 다시 시작하는 게 싸다

---

## 4. 리스크와 버퍼

| 리스크 | 대비 |
|---|---|
| Apple 리젝 반복 | 버퍼 4주 + 리젝 단골 사유(익명 UGC)를 9월에 선제 구현 |
| 8월 전환 지연 (최대 공사) | 9월 1주차를 이월 버퍼로 사용 가능 (온보딩은 뒤로 밀어도 무방) |
| 크롤러 차단/구조 변경 | 저속·저빈도라 위험 낮음. 실패 시 상학부만 수동 구축(협력자 분담)으로 축소 |
| 시딩 콘텐츠 부족 | 12월 베타 = 시딩 겸용. 협력자 확보를 11월부터 시작 (개발과 병행) |
| 시험기간 (7월 말, 1월 말) | 해당 주간은 세션 0회로 계산에 미포함 |
| Expo SDK 메이저 업데이트 | 12월 동결 전까지만 추종, 이후 고정 |

---

## 5. 지금 결정되어 있는 것 (재론 금지 목록)

- 익명성 유지 (단, 운영자는 투고자 특정 가능 — 법적 대응용 `authorUid` 보존)
- 이중 공간: 유학생 라운지(전국·개방) / 학교 게시판(단계별 인증)
- 인증은 verificationLevel 스위치 방식 — 초기 개방, 성장기에 조임
- 강의평은 구조화(별점·태그) + 자유텍스트 분리 (언어권 확장 대비)
- Penmark 유틸리티 따라하기(캘린더·투두) 금지
- 확산 축: 한국인 유학생 → 전체 유학생. 일본인은 다리 기능으로만
- **로그인 ≠ 대학인증** (2026-07-14 확정): 사용자의 본체는 uid. 이메일 등록(아무 메일 OK)은
  데이터 인계용 열쇠, Microsoft/학교메일 인증은 배지(verificationLevel) 획득 경로.
  두 경로를 합치지 않는다 — 대학 계정은 졸업 시 회수되므로 uid를 대학 계정에만 묶으면
  졸업생 데이터 접근이 끊김. 게스트→등록→인증의 3단 구조 유지
- **시간표 기본 공개범위 = friends** (2026-07-15 확정): 친구 시간표 열람·겹침 표시가
  앱의 핵심 가치이므로 기본값은 "친구에게 공개". 본인이 Phase 2 공개범위 UI에서
  private/department/public으로 변경 가능. 개인정보처리방침의 "본인이 공개 설정한
  시간표" 조항과 일치
- **공개 정보 분리** (2026-07-15 확정): users 본체(이메일·학업정보 포함)는 본인 전용,
  타인에게 보이는 정보는 usersPublic(닉네임·사진·학교·학부)만. GPA 등은 절대 미러 금지
- **학점 관리(졸업요건 추적) = 런칭 1.0 범위 밖 · 2-트랙 개발** (2026-07-21 확정):
  설계·상학부 마스터 추출은 완료([docs/CREDIT-TRACKING.md](CREDIT-TRACKING.md),
  [docs/data/meiji-commerce-2026.json](data/meiji-commerce-2026.json))이나 **구현은 런칭 후(Phase 3)**.
  결정 골자:
  - **3 데이터 소스 분리:** 便覧=룰북(요건·코스별 과목·단위 마스터) / 성적표=개인 이수 이력 /
    시라버스=실시간 개설(이미 크롤러 보유). 이 셋을 합쳐야 "학점 관리"가 됨.
  - **마스터 획득 2-트랙:**
    - **트랙 A (LLM 보조 authoring):** 便覧 PDF → LLM 초안 → **개발자/큐레이터 검수 → 공유 마스터(verified)**.
      LLM은 **오프라인 저작 보조만**(런타임 아님). 마스터는 `(학교×학부×입학년도)` 단위 · 소수 · 거의 불변 →
      **자동화가 아니라 재사용으로 확장.** 便覧은 규칙 파서 불가(세로표+학교마다 상이)라 LLM 초안이 정석.
    - **트랙 B (사용자 수작업):** 성적표 이수 이력은 **학생 手입력**(만능 폴백, 옵션: 텍스트레이어 PDF 규칙 파싱).
      미커버 학부 마스터는 학생이 비공개 手작성 → 검수 후 공유 승격.
  - **런타임은 결정론적 계산(LLM 0)** — 고위험 데이터라 감사 가능·프라이버시(성적 기기 밖 미전송) 우선.
    마스터 신뢰는 기존 강의 데이터의 `verified`/`confirmCount` 메커니즘 재사용.
  - **커버리지 전략:** 런칭 타깃 학교는 **공개 履修要項으로 선제 시딩**, 나머지는 업로드/크라우드 기여로 확장.
    큐레이터 = 초기 개발팀 → 성장기 "작성"에서 "승인(검수)"으로 역할 이동.
